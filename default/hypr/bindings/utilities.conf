# Menus
bindd = SUPER, SPACE, Launch apps, exec, collectiveos-launch-walker
bindd = SUPER CTRL, E, Emoji picker, exec, collectiveos-launch-walker -m symbols
bindd = SUPER ALT, SPACE, CollectiveOS menu, exec, collectiveos-menu
bindd = SUPER, ESCAPE, System menu, exec, collectiveos-menu system
bindld = , XF86PowerOff, Power menu, exec, collectiveos-menu system
bindd = SUPER, K, Show key bindings, exec, collectiveos-menu-keybindings
bindd = , XF86Calculator, Calculator, exec, gnome-calculator

# Aesthetics
bindd = SUPER SHIFT, SPACE, Toggle top bar, exec, collectiveos-toggle-waybar
bindd = SUPER CTRL, SPACE, Next background in theme, exec, collectiveos-theme-bg-next
bindd = SUPER SHIFT CTRL, SPACE, Theme menu, exec, collectiveos-menu theme
bindd = SUPER, BACKSPACE, Toggle window transparency, exec, hyprctl dispatch setprop "address:$(hyprctl activewindow -j | jq -r '.address')" opaque toggle
bindd = SUPER SHIFT, BACKSPACE, Toggle workspace gaps, exec, collectiveos-hyprland-workspace-toggle-gaps

# Notifications
bindd = SUPER, COMMA, Dismiss last notification, exec, makoctl dismiss
bindd = SUPER SHIFT, COMMA, Dismiss all notifications, exec, makoctl dismiss --all
bindd = SUPER CTRL, COMMA, Toggle silencing notifications, exec, makoctl mode -t do-not-disturb && makoctl mode | grep -q 'do-not-disturb' && notify-send "Silenced notifications" || notify-send "Enabled notifications"

# Toggle idling
bindd = SUPER CTRL, I, Toggle locking on idle, exec, collectiveos-toggle-idle

# Toggle nightlight
bindd = SUPER CTRL, N, Toggle nightlight, exec, collectiveos-toggle-nightlight

# Control Apple Display brightness
bindd = CTRL, F1, Apple Display brightness down, exec, collectiveos-cmd-apple-display-brightness -5000
bindd = CTRL, F2, Apple Display brightness up, exec, collectiveos-cmd-apple-display-brightness +5000
bindd = SHIFT CTRL, F2, Apple Display full brightness, exec, collectiveos-cmd-apple-display-brightness +60000

# Captures
bindd = SUPER SHIFT, S, Screenshot with editing, exec, collectiveos-cmd-screenshot
bindd = SHIFT, PRINT, Screenshot to clipboard, exec, collectiveos-cmd-screenshot smart clipboard
bindd = ALT, PRINT, Screenrecording, exec, collectiveos-menu screenrecord
bindd = SUPER, PRINT, Color picker, exec, pkill hyprpicker || hyprpicker -a

# File sharing
bindd = SUPER CTRL, S, Share, exec, collectiveos-menu share

# Waybar-less information
bindd = SUPER CTRL, T, Show time, exec, notify-send "    $(date +"%A %H:%M  —  %d %B W%V %Y")"
bindd = SUPER CTRL, B, Show battery remaining, exec, notify-send "󰁹    Battery is at $(collectiveos-battery-remaining)%"
