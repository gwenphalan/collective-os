#!/bin/bash

# Call the VSCode theme setter with Cursor-specific parameters
collectiveos-theme-set-vscode cursor "$HOME/.config/Cursor/User/settings.json" "$HOME/.local/state/collectiveos/toggles/skip-cursor-theme-changes" Cursor
